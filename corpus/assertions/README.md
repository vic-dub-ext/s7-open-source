Per-paper acceptance assertions live here.
Each file is `<paper_key>.yaml` with `match`/`expect` blocks checked
against the published `association_records`, derived from the papers' own
abstracts and main-text tables.

No assertion files are defined yet; the format above is the intended one.
